/*function PanelCard({ title, children, marginBottom = "24px" }) {
    return (
        <div
            style={{
                backgroundColor: "#111827",
                padding: "24px",
                borderRadius: "16px",
                marginBottom
            }}
        >
            <h3 style={{ marginTop: 0, marginBottom: "16px" }}>{title}</h3>
            {children}
        </div>
    )
}

export default PanelCard
*/